apiVersion: v1
kind: Service
metadata:
  name: boardapp-service
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: boardapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080